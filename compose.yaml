services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: crewledger
      POSTGRES_USER: crewledger
      POSTGRES_PASSWORD: crewledger
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
