info:
  name: Protected Route
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/api/campaigns"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
