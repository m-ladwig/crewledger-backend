info:
  name: Register
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "testgm2",
        "email": "testgm2@example.com",
        "password": "password123",
        "role": "GM"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
